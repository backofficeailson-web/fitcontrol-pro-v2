import { useEffect, useMemo, useState } from 'react'; import { Link } from 'react-router-dom'; import { api } from '../services/api';
export default function Alunos(){ const [items,setItems]=useState([]),[q,setQ]=useState(''); useEffect(()=>{api('/alunos').then(setItems)},[]); const list=useMemo(()=>items.filter(a=>(a.nome||'').toLowerCase().includes(q.toLowerCase())),[items,q]); return <><div className="title"><h2>Alunos</h2><Link className="button" to="/alunos/novo">Novo aluno</Link></div><input placeholder="Buscar aluno" value={q} onChange={e=>setQ(e.target.value)}/><div className="table">{list.map(a=><Link key={a.id} to={`/alunos/${a.id}`}><b>{a.nome}</b><span>{a.modalidadePrincipal}</span><span className="badge">{a.status||'ativo'}</span></Link>)}</div></> }
