import { useState } from 'react';
import { Link, useNavigate } from 'react-router-dom';
import { useAuth } from '../context/AuthContext';
export function Login(){ const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const [error,setError]=useState(''); const nav=useNavigate(); const {login}=useAuth(); async function submit(e){ e.preventDefault(); setError(''); try{ await login(email,password); nav('/dashboard'); }catch(err){setError(err.message)} } return <AuthBox title="Entrar"><form onSubmit={submit}><input placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)}/><input placeholder="Senha" type="password" value={password} onChange={e=>setPassword(e.target.value)}/>{error&&<p className="error">{error}</p>}<button>Entrar</button><Link to="/register">Criar conta</Link></form></AuthBox> }
export function Register(){ const [form,setForm]=useState({nome:'',email:'',password:''}); const [error,setError]=useState(''); const nav=useNavigate(); const {register}=useAuth(); async function submit(e){ e.preventDefault(); try{ await register(form); nav('/dashboard'); }catch(err){setError(err.message)} } return <AuthBox title="Criar conta"><form onSubmit={submit}><input placeholder="Nome" onChange={e=>setForm({...form,nome:e.target.value})}/><input placeholder="Email" onChange={e=>setForm({...form,email:e.target.value})}/><input placeholder="Senha" type="password" onChange={e=>setForm({...form,password:e.target.value})}/>{error&&<p className="error">{error}</p>}<button>Cadastrar</button><Link to="/login">Já tenho conta</Link></form></AuthBox> }
function AuthBox({title,children}){ return <div className="auth"><div className="auth-card"><h1>{title}</h1><p>FitControl Pro 2.0</p>{children}</div></div> }
