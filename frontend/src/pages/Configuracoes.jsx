import { useState } from 'react'; import { api } from '../services/api';
export default function Configuracoes(){ const [json,setJson]=useState(''); async function exp(){ const data=await api('/export'); const blob=new Blob([JSON.stringify(data,null,2)],{type:'application/json'}); const a=document.createElement('a'); a.href=URL.createObjectURL(blob); a.download='fitcontrol-backup.json'; a.click(); } async function imp(){ await api('/import',{method:'POST',body:JSON.parse(json)}); alert('Importado'); } return <><h2>Configurações</h2><div className="panel"><button onClick={exp}>Exportar backup JSON</button><textarea placeholder="Cole o JSON para importar" value={json} onChange={e=>setJson(e.target.value)}/><button onClick={imp}>Importar JSON</button></div></> }
