import { useEffect, useState } from 'react'; import { useParams } from 'react-router-dom'; import { api } from '../services/api';
export default function TreinoDetalhe(){ const {id}=useParams(); const [t,setT]=useState(null); useEffect(()=>{api(`/treinos/${id}`).then(setT)},[id]); if(!t) return <p>Carregando...</p>; return <><div className="title"><h2>{t.nome||'Treino'}</h2><button onClick={()=>window.print()}>Imprimir</button></div><div className="panel"><p><b>Objetivo:</b> {t.objetivo}</p><p><b>Modalidade:</b> {t.modalidade}</p><p><b>Divisão:</b> {t.divisao}</p><p>{t.observacoes}</p></div><div className="table">{(t.exercicios||[]).map((e,i)=><div key={i}><b>{e.nome}</b><span>{e.series}x{e.repeticoes}</span><span>{e.descanso}</span><span>RPE {e.rpe}</span></div>)}</div></> }
